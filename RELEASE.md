# Руководство по релизам

## Настройка Travis CI

### 1. Подключение репозитория к Travis CI

1. Зайдите на [travis-ci.com](https://travis-ci.com)
2. Авторизуйтесь через GitHub
3. Активируйте репозиторий `b1411/parallel.js`

### 2. Настройка переменных окружения

В настройках репозитория на Travis CI добавьте:

- `NPM_TOKEN` - токен для публикации в npm (получить на npmjs.com → Access Tokens)
- `NPM_EMAIL` - ваш email для npm
- `GH_TOKEN` - GitHub Personal Access Token (для создания релизов)

**Как получить NPM_TOKEN:**
```bash
npm login
npm token create
```

**Как получить GH_TOKEN:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выберите права: `repo`, `write:packages`

### 3. Формат коммитов (Conventional Commits)

Используйте правильный формат коммитов для автоматической генерации версий:

```
feat: добавлена новая функция       # minor версия (1.1.0 → 1.2.0)
fix: исправлена ошибка              # patch версия (1.1.0 → 1.1.1)
docs: обновлена документация        # не меняет версию
chore: обновлены зависимости        # не меняет версию

BREAKING CHANGE: изменен API        # major версия (1.1.0 → 2.0.0)
```

## Workflow релизов

### Разработка новой фичи

```bash
# 1. Создаем ветку для фичи
git checkout -b feature/new-awesome-feature

# 2. Делаем изменения и коммитим
git add .
git commit -m "feat: add awesome new feature"

# 3. Пушим и создаем PR в develop
git push origin feature/new-awesome-feature
```

### Подготовка релиза

```bash
# 1. Создаем PR из develop в main
# Все фичи из develop попадут в main

# 2. После мерджа в main:
# - Travis автоматически запустит тесты
# - Создаст новый релиз (semantic-release)
# - Обновит CHANGELOG.md
# - Опубликует в npm
# - Создаст GitHub Release с тегом
```

### Hotfix (срочное исправление)

```bash
# 1. Создаем ветку от main
git checkout main
git pull
git checkout -b hotfix/critical-bug

# 2. Исправляем и коммитим
git add .
git commit -m "fix: resolve critical bug in thread pool"

# 3. Создаем PR сразу в main
git push origin hotfix/critical-bug

# 4. После мерджа релиз создастся автоматически
```

## Что происходит при мердже в main

1. ✅ Travis запускает тесты на Node 18, 20, 22
2. ✅ Проверяет линтинг
3. ✅ Собирает проект
4. ✅ Анализирует коммиты и определяет новую версию
5. ✅ Генерирует CHANGELOG.md
6. ✅ Обновляет версию в package.json
7. ✅ Публикует в npm
8. ✅ Создает GitHub Release с тегом
9. ✅ Делает коммит с обновленными файлами

## Ручной релиз (если нужно)

```bash
# Установите semantic-release локально
npm install -D semantic-release @semantic-release/changelog @semantic-release/git

# Запустите релиз
NPM_TOKEN=your_token GH_TOKEN=your_token npm run release
```

## Проверка перед релизом

```bash
# Убедитесь что все тесты проходят
npm test

# Проверьте линтинг
npm run lint

# Проверьте сборку
npm run build

# Проверьте что версия корректная
npm version
```

## Структура веток

```
main (защищена)
  ↑
  └── develop
       ↑
       ├── feature/feature-1
       ├── feature/feature-2
       └── bugfix/bug-fix
```

## Рекомендации

- 🔒 Защитите `main` ветку от прямых пушей (Settings → Branches → Branch protection rules)
- ✅ Требуйте успешного прохождения CI перед мерджем
- 👥 Требуйте code review для PR в main
- 📝 Используйте Conventional Commits всегда
- 🏷️ Не создавайте теги вручную - semantic-release делает это автоматически
